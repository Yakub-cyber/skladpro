-- ── Назначение заказа курьеру-сотруднику ─────────────────────────────────────
-- Колонка assigned_to хранит id сотрудника-курьера, на которого назначен заказ.
-- Курьер в приложении видит только заказы, где assigned_to = его id.
-- Запустите в Supabase → SQL Editor → Run. Идемпотентно (можно повторно).

alter table public.orders add column if not exists assigned_to text;

-- Готово. Назначайте курьеров в разделе «Заказы» (выпадающий список «Курьер»).
